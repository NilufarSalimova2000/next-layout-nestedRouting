import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
      domains: ["raw.githubusercontent.com"], // Rasmlar yuklanadigan xostni qo'shish
    },
  /* config options here */
};

export default nextConfig;
